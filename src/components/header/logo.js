import React from 'react';
import brandLogo from './brandLogo.png';

const Logo = () => {
    return (
        <div>
            <img alt="logo" width="50px" src={brandLogo} />
        </div>
    );
}

export default Logo;
